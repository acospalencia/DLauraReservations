@model List<Promotion>
@{
    ViewData["Title"] = "Promociones";
}

<div class="promotions-container">
    <h1>Promociones Disponibles</h1>

    @if (Model != null && Model.Any())
    {
        <div class="promotion-grid">
            @foreach (var promotion in Model)
            {
                <div class="promotion-item @promotion.CssClass">
                    <img src="@promotion.ImageUrl" alt="@promotion.AltText" class="promotion-image" />
                    <div class="promotion-details">
                        @if (!string.IsNullOrEmpty(promotion.Title))
                        {
                            <h3>@promotion.Title</h3>
                        }
                        @if (!string.IsNullOrEmpty(promotion.Description))
                        {
                            <p>@promotion.Description</p>
                        }
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="no-promotions">
            <p>Próximamente tendremos nuevas promociones para ti</p>
            <div class="promotion-grid">
                <div class="promotion-item">
                    <div class="placeholder-image"></div>
                    <div class="promotion-details">
                        <h3>Ejemplo 1</h3>
                        <p>Déjate llevar por una experiencia gastronómica única en Doña Laura. Nuestros chefs fusionan ingredientes frescos y técnicas innovadoras para deleitar tu paladar con platillos exquisitos.</p>
                    </div>
                </div>
                <div class="promotion-item">
                    <div class="placeholder-image"></div>
                    <div class="promotion-details">
                        <h3>Ejemplo 2</h3>
                        <p>¿Tienes hambre y poco tiempo? En Doña Laura te ofrecemos comida rápida con sabor casero. Desde nuestras hamburguesas jugosas hasta los tacos más sabrosos, todo está hecho con ingredientes de calidad y al momento.</p>
                    </div>
                </div>
                <div class="promotion-item">
                    <div class="placeholder-image"></div>
                    <div class="promotion-details">
                        <h3>Ejemplo 3</h3>
                        <p> En Restaurante Doña Laura, la buena comida y los buenos momentos van de la mano. Nuestro menú está pensado para toda la familia, con platillos deliciosos preparados con ingredientes frescos y mucho cariño..</p>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

<style>
    .promotions-container {
        padding: 20px;
        max-width: 1200px;
        margin: 0 auto;
    }

    .promotion-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 30px;
        margin-top: 20px;
    }

    .promotion-item {
        border: 1px solid #eee;
        border-radius: 8px;
        overflow: hidden;
        background-color: #fff;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        transition: transform 0.3s ease;
    }

        .promotion-item:hover {
            transform: translateY(-5px);
        }

    .promotion-image {
        width: 100%;
        height: 180px;
        object-fit: cover;
    }

    .promotion-details {
        padding: 15px;
    }

        .promotion-details h3 {
            margin: 0 0 10px 0;
            color: #333;
            font-size: 1.1rem;
        }

        .promotion-details p {
            margin: 0;
            color: #666;
            font-size: 0.9rem;
            line-height: 1.4;
        }

    .no-promotions {
        text-align: center;
        color: #666;
    }

    .placeholder-image {
        height: 180px;
        background-color: #f5f5f5;
        display: flex;
        align-items: center;
        justify-content: center;
    }
</style>